(library
 ((name        bfx_rest)
  (public_name bfx-rest)
  (modules (bfx_rest))
  (preprocess (pps (ppx_sexp_conv)))
  (libraries (bfx
              core
              async
              yojson
              ocplib-json-typed
              cohttp-async
              base64
              digestif.c
              hex))))
