(library
 ((name        bfx_ws)
  (public_name bfx-ws)
  (modules (bfx_ws))
  (preprocess (pps (ppx_sexp_conv)))
  (libraries (bfx core async yojson ocplib-json-typed
                   bs-devkit cohttp-async nocrypto hex
                   websocket-async))))